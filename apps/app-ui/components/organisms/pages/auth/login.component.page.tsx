'use client';

import { LoginForm } from '@components/molecules';
import React, { FunctionComponent } from 'react';

export const LoginPage: FunctionComponent = () => (
  <LoginForm title="Welcome to Example App" />
);

export default LoginPage;
